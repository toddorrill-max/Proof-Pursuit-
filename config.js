// Owner-editable settings. Production data is intentionally empty until verified.
export const brand = Object.freeze({
  name: 'Proof Pursuit',
  tagline: 'The Bourbon Hunter',
  headline: 'Master the Hunt. Outsmart the Hype.',
  description: 'A thoughtful field guide to allocated bourbon, Ohio hunting, barrel picks, and buying with confidence.',
  mark: 'PP'
});
export const dataConfig = Object.freeze({ mode: 'demo' });
export const freshnessRules = Object.freeze({ freshHours: 24, agingHours: 72 });
export const confidenceLevels = Object.freeze(['High', 'Medium', 'Low']);
